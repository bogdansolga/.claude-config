{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'echo \"$CLAUDE_TOOL_INPUT\" | grep -qiwE \"(INSERT|UPDATE|DELETE|DROP|TRUNCATE|ALTER)\" && echo \"DENY: SQL write operations not allowed\" && exit 1 || exit 0'"
          }
        ]
      }
    ]
  }
}
